<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gamepad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TicActivityPvc"
            android:parentActivityName=".TicActivityHome"/>
        <activity
            android:name=".TicActivityHome"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".HangActivity"
            android:label="Hangman"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".TicActivityPvp"
            android:label="TicTacToe"
            android:parentActivityName=".TicActivityHome" />
        <activity
            android:name=".MainActivity"
            android:label="GamePad">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>